# Copy to config/ros.env and edit if needed.
export ROS_DOMAIN_ID=42
export ROS_LOCALHOST_ONLY=0

# If LAN multicast is unreliable, keep hardware control on the Jetson and use SSH.
# Add explicit DDS/unicast configuration only after local ROS operation is proven.
